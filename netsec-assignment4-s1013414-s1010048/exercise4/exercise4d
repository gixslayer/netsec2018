The ICMP Error should come as a response to prior traffic, meaning there should already be an active mapping in the NAT. If this mapping is not there, the ICMP Error makes no sense, and thus is silently dropped.
